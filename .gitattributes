*.wav filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.ico filter=lfs diff=lfs merge=lfs -text

docs/_static/*.png -filter -diff -merge -text # these two are being troublesome, everything else is working fine
docs/_static/*.ico -filter -diff -merge -text # these two are being troublesome, everything else is working fine

pmma/extern/** linguist-vendored
